<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
          x:Class="SampleBrowser.GroupingSampleItemTemplate">

    <ViewCell.View>
        <Grid BackgroundColor="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Margin="5,0,0,0" FontSize="15" Text="{Binding Title}" TextColor="Black" VerticalOptions="Center" />

            <Label Grid.Column="1" Text="in" TextColor="Gray" VerticalOptions="Center" />

            <Label Grid.Column="2" Text="{Binding Control}" TextColor="Gray" VerticalOptions="Center" />
        </Grid>
    </ViewCell.View>
</ViewCell>