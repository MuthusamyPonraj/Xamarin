<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
          xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
          x:Class="SampleBrowser.GroupingControlItemTemplate">

    <ViewCell.View>
        <Grid x:Name="grid" RowSpacing="0" VerticalOptions="FillAndExpand" WidthRequest="300" BackgroundColor="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Image Source="{Binding ImageId}" Margin="3,6,10,6" Aspect="Fill" VerticalOptions="CenterAndExpand" HorizontalOptions="FillAndExpand" />

            <Label FontSize="15" Margin="0,6,0,3" HorizontalOptions="StartAndExpand" TextColor="Black" Grid.Column="1"
                   Text="{Binding Title}" HorizontalTextAlignment="Start" VerticalTextAlignment="Center" />
        </Grid>
    </ViewCell.View>
</ViewCell>